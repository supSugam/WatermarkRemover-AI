# These are the proven versions from the standalone setup.sh
# Core ML
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.50.0
diffusers>=0.30.0
numpy<2

# Image Processing
opencv-python-headless>=4.8.0,<4.12.0
Pillow>=10.0.0

# Utilities
loguru
click
tqdm
psutil
pyyaml

# iopaint helper dependencies (installed explicitly because iopaint is installed with --no-deps)
pydantic
typer
einops
omegaconf
easydict
yacs
