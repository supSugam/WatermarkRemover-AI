# Core AI/ML - CUDA 12.4 versions
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0
# Must be pinned: newer diffusers requires peft>=0.17.0 but iopaint pins peft==0.7.1
diffusers>=0.27.0,<0.29.0
# Must be pinned: iopaint was designed for transformers 4.x
transformers>=4.48.0,<5.0.0
numpy<2

# Image/Video processing
opencv-python-headless>=4.8.0,<4.12.0
Pillow>=10.0.0

# Inpainting core (iopaint installed separately with --no-deps to avoid resolver conflicts)

# Utilities
loguru
click
tqdm
psutil
pyyaml
