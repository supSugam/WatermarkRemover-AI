# PyTorch (CUDA 12.4 wheels) — must come before iopaint so the right torch is used
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0

# Inpainting backend
# iopaint has rigid exact-version pins (pillow==9.5.0, peft==0.7.1, diffusers 0.27.x, etc.)
# Let it own all those transitive deps — do NOT add conflicting explicit constraints below.
iopaint

# OpenCV headless (iopaint does not pin this)
opencv-python-headless>=4.8.0,<4.12.0

# numpy compat guard (shared constraint — iopaint also needs numpy<2)
numpy<2

# Utilities not pulled in by iopaint
loguru
click
tqdm
psutil
pyyaml
